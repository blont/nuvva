{{ 'section-accordion.css' | asset_url | stylesheet_tag }}



<div class="accordion-ctn">
    {% if section.settings.title %}
    <h2>
    {{ section.settings.title }}
 </h2>
    {% endif %}


<div class="row">
    <div class="col">

    <div class="tabs">
      
        {% for block in section.blocks %}
            {% assign count = count | plus: 1 %}
            {% assign delay = delay | plus: 200 %}
            <div class="tab" data-aos="fade-up" data-aos-delay="{{ delay }}"  data-aos-duration="1100">
    
        <input type="checkbox" id="chck{{ count }}">
      
        {% if block.settings.title %}

          <label class="tab-label" for="chck{{ count }}">{{ block.settings.title }}</label>
          {% endif %}
      
      
        <div class="tab-content">
          {% if block.settings.content %}

      <p>      {{ block.settings.content }}</p>
            {% endif %}


        </div>
      </div>

    {% endfor %}


    </div>

</div>

</div>

</div>




{% schema %}{
    "name": "Accordions",
    "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Optional Header",
          "default": "Accordion Header"
        }
      ],

      "blocks": [
        {
          "type": "accordion",
          "name": "Accordion",
          "settings": [
            {
              "type": "text",
              "id": "title",
              "default": "Accordion Header",
              "label": "Accordion Header"
            },
            {
              "type": "richtext",
              "id": "content",
        
              "label": "Accordion Content"
            }
          ]
   }

  ],
"presets": [
{
"name":"Accordions"
}
]
}
{% endschema %}
